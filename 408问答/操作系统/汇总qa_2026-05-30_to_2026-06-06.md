# 操作系统 · 问答汇总

> 共 **20 题** | 2026-05-30 → 2026-06-06
> 来源：[2026_05_30_to_2026_06_06](2026_05_30_to_2026_06_06.md)

---

## 一、章节分布

| 章节 | 题数 | 占比 | 对应 Q 号 |
| :--- | :---: | :---: | :--- |
| **操作系统概述**（系统调用、内核态/用户态、OS 结构、中断/异常） | 8 | 40% | Q1–Q3, Q5, Q6, Q9–Q11 |
| **进程管理**（线程模型、进程阻塞、进程状态、信号、调度时间、进程同步） | 10 | 50% | Q7, Q8, Q12–Q16, Q18–Q20 |
| **I/O 管理**（I/O 软件层次、I/O 生命周期、中断/DMA/CPU并行） | 2 | 10% | Q4, Q17 |
| **总计** | **20** | 100% | |

---

## 二、按章节逐题汇总

### 第 1 章 · 操作系统概述（Q1–Q3, Q5, Q6, Q9–Q11）

| Q# | 类型 | 关键词 | 问题 | 核心结论 |
| :---: | :---: | :--- | :--- | :--- |
| Q1 | 概念问题 | 系统调用/系统命令 | 系统调用是否等同于系统命令？ | 不是。系统调用是内核原子功能单元（核心态），系统命令是可执行程序（用户态），一条命令通常调用多个系统调用 |
| Q2 | 概念问题 | 系统调用发生位置 | 系统调用到底在哪里发生？能否说在用户态发生？ | 可以。408 真题定调：系统调用"在用户态发生"指用户态执行陷入指令发起请求；处理阶段转入内核态。发起在用户态，处理在内核态 |
| Q3 | 概念问题 | 特权指令 | 哪些指令/事件不能在用户态执行？ | 用户态只能做计算、访自己内存、发系统调用。I/O、关中断、改页表、改控制寄存器、停机等均为特权指令，必须在核心态执行 |
| Q5 | 总结 | 硬件/OS 分工 | 系统调用/中断/异常中，硬件和 OS 分别做了什么？ | 硬件：保存 PC/PSW、切换栈、查向量表、跳转 ISR。OS：保存通用寄存器、执行处理逻辑、恢复上下文、中断返回。分工表覆盖三种事件 |
| Q6 | 概念问题 | 中断/子程序/系统调用/库函数 | 中断 vs 子程序调用有何区别？系统调用 vs 库函数有何区别？ | 中断：硬件触发，异步，内核态处理。子程序：软件调用，同步，同态。系统调用：跨边界（用户→内核）。库函数：全程用户态 |
| Q9 | 概念问题 | 外核 Exokernel | 什么是外核？与微内核有何区别？ | 外核只做资源隔离与复用（不抽象），将硬件资源直接暴露给应用层库 OS。微内核做最小化抽象+IPC 通信。外核性能最高、隔离靠硬件 |
| Q10 | 概念问题 | 用户态/内核态 | 用户态与内核态的本质区别是什么？如何切换？ | 本质区别：CPU 特权级别决定可执行的指令集合。切换方式：用户→内核通过 trap/中断/异常；内核→用户通过中断返回指令。PSW 中 mode bit 控制 |
| Q11 | 概念问题 | VMM 类型/虚拟化 | 两种 VMM（Type 1 vs Type 2）中 Guest OS 的用户态与内核态如何运行？ | Type 1（裸机型）：Guest OS 内核运行在非根模式（deprivileged），特权指令被 trap 到 VMM。Type 2（宿主型）：Guest OS 是宿主机上的用户进程。两种情况下 Guest OS 的"内核态"都被降权 |

---

### 第 2 章 · 进程管理（Q7, Q8, Q12–Q16, Q18–Q20）

| Q# | 类型 | 关键词 | 问题 | 核心结论 |
| :---: | :---: | :--- | :--- | :--- |
| Q7 | 概念问题 | 时钟中断 ISR | 时钟中断服务程序需要更新哪些内容？ | 更新：①当前进程时间片计数器 ②系统全局时间 ③定时器链表检查与唤醒 ④CPU 使用时间统计 ⑤就绪/运行队列调整（时间片耗尽触发调度） |
| Q8 | 概念问题 | 信号 Signal | 信号（Signal）的核心概念是什么？ | 信号是 OS 的软件中断机制——异步通知进程某事件发生。生命周期：生成→递送→处理（默认动作/忽略/用户注册 handler）。sigaction 可精确控制处理行为 |
| Q12 | 概念问题 | KLT/一对一模型 | 纯内核级线程（KLT）与组合方式一对一模型（ULT+KLT）有何区别？ | 核心区别仅在于用户空间是否有线程库层。纯 KLT：App 直接 syscall 创建 KLT。一对一：线程库维护 ULT 描述符，再通过 clone() 创建对应 KLT。两者的内核 TCB 和调度行为完全一致 |
| Q13 | 总结 | 进程阻塞事件 | 哪些事件会导致进程从 Running → Blocked？ | 9 类阻塞事件：I/O 请求、资源申请失败、等待子进程、进程间同步、消息通信、定时/睡眠、缺页中断、用户输入、网络 I/O。Run→Blocked 必为主动调用。**部分事件有条件**：申请临界资源仅在被占用时阻塞，read() 仅在 cache miss 时阻塞 |
| Q14 | 概念问题 | 读磁盘本质 | 进程读磁盘是激活另一个进程，还是进程自带的能力？ | 都不是。读磁盘 = 进程通过 read() 系统调用委托 OS 内核完成。三角色：进程发起请求（用户态）→ 内核驱动硬件（内核态）→ 磁盘控制器执行 DMA 传输（硬件）。进程在 I/O 期间 Blocked，不占 CPU |
| Q15 | 概念问题 | ULT 双层管理/三种线程模型 | 用户级线程中线程管理在用户空间，但进程仍由内核创建——正确吗？ | 完全正确。进程是内核创建的"容器"（PCB），线程是用户空间线程库创建的"内容"（TCB）。补全三种线程模型对比：纯 ULT（多对一，切换快但一阻塞全进程陪葬）、纯 KLT、一对一（取两者之长）。ULT 致命缺陷：一个线程阻塞系统调用 → 内核阻塞整个进程 |
| Q16 | 概念问题 | 就绪队列/到达时间/周转时间 | 进程进入就绪队列时，是否可以开始计算周转时间？ | 在 408 考试语境下，是——New→Ready 延迟为 0，就绪时刻即为到达时间。但须区分三种进入就绪队列的场景：只有 New→Ready 是"到达"；Running→Ready（被抢占）和 Blocked→Ready（I/O 完成）都不是新的到达，周转时间不重置 |
| Q18 | 概念问题 | TestAndSet/共享变量/锁机制 | TestAndSet 中 lock 变量为什么叫"共享"变量？是控制所有资源吗？ | "共享"指存储属性（共享内存中，所有进程可见），非管辖范围。每个临界资源一把独立锁——N 个资源需要 N 个共享布尔变量。混淆根源：将锁的"可见性"误读为锁的"管辖范围" |
| Q19 | 总结 | 同步机制全景/事件驱动vs时间线 | OS 同步机制如何分类？互斥锁/信号量/管程（事件驱动）与屏障/锁步（时间线型）有何本质区别？ | 两大类：事件驱动型（锁/信号量/管程）通过共享变量状态变化协调——"等条件就绪"；时间线型（屏障/锁步）通过执行进度协调——"等所有人到齐"。互斥=空间隔离，屏障=时间对齐。屏障标准实现依赖信号量，体现两种范式可组合而非对立 |
| Q20 | 概念问题 | PV命名/wait/signal/同名陷阱 | PV 操作是不是就是指 wait() 和 signal() 操作？ | 是的。P=wait=down=acquire，V=signal=up=release，同一操作的不同命名体系，408 考试混用不扣分。警惕同名陷阱：信号量的 wait/signal（改计数器）≠ 管程条件变量的 wait/signal（释放管程锁+阻塞），区分看操作对象 |

---

### 第 5 章 · I/O 管理（Q4, Q17）

| Q# | 类型 | 关键词 | 问题 | 核心结论 |
| :---: | :---: | :--- | :--- | :--- |
| Q4 | 总结 | I/O 软件层次/I/O 生命周期 | 一次完整的 I/O 操作包含哪些阶段？OS 各层做了什么？ | I/O 四层软件：用户层 I/O → 设备无关软件层 → 设备驱动程序 → 中断处理程序。三阶段：I/O 请求（发起+排队）→ I/O 进行（驱动+DMA）→ I/O 完成（中断唤醒） |
| Q17 | 概念问题 | 中断瞬间性/DMA/CPU-I/O并行 | CPU 的计算被中断打断只有一瞬间，其余时间都在计算——正确吗？ | 完全正确。DMA 控制器承担 I/O 数据搬运（CPU 不参与），I/O 完成时硬件发中断，CPU 执行微秒级 ISR 后立即恢复计算。中断 vs 阻塞 vs 上下文切换三种暂停的时间量级完全不同：中断微秒级、上下文切换微秒~几十微秒、进程阻塞毫秒~秒级（但 CPU 此时在运行其他进程） |

---

## 三、按类型分布

| 类型 | 题数 | Q 号 | 说明 |
| :--- | :---: | :--- | :--- |
| **概念问题** | 16 | Q1, Q2, Q3, Q6, Q7, Q8, Q9, Q10, Q11, Q12, Q14, Q15, Q16, Q17, Q18, Q20 | 概念定义、概念辨析、机制解释 |
| **总结** | 4 | Q4, Q5, Q13, Q19 | 知识汇总表/流程图/分类清单 |

> Q5 横跨概念辨析与分工总结，归入总结类。Q7 虽有总结性质（更新内容清单），但问题本质是"时钟 ISR 做什么"，归入概念问题。Q17 辨析中断/阻塞/上下文切换三种暂停的时间量级与本质差异，归入概念问题。Q18 辨析 TestAndSet 中 lock 变量"共享"（可见性）与"管辖范围"的混淆，归入概念问题。Q19 全景对比事件驱动型与时间线型同步机制（含代码实现与对比表），归入总结类。Q20 辨析 PV/wait/signal 命名等价性及管程与信号量中的同名陷阱，归入概念问题。

---

## 四、高频考点索引

| 考点 | 涉及 Q 号 | 掌握程度 |
| :--- | :--- | :---: |
| 系统调用（定义/发生位置/与命令区别/与库函数区别） | Q1, Q2, Q6 | ●●●●● |
| 用户态与内核态（切换机制、特权指令、VMM 中的态） | Q3, Q10, Q11 | ●●●●● |
| 中断与异常（硬件/OS 分工、中断 vs 子程序） | Q5, Q6 | ●●●● |
| 进程状态转换（阻塞事件、Run→Blocked 触发条件） | Q13, Q14 | ●●●●● |
| 调度时间概念（到达/周转/等待/响应时间、三种就绪场景） | Q16 | ●●●●● |
| 线程模型（ULT 多对一、KLT、一对一） | Q12, Q15 | ●●●●● |
| 进程同步与互斥（TestAndSet/信号量/管程/屏障/PV命名 全景） | Q18, Q19, Q20 | ●●●●● |
| I/O 软件层次与生命周期 | Q4 | ●●●● |
| 中断处理与 DMA（CPU-I/O 并行、中断/阻塞/切换时间量级） | Q17 | ●●●●● |
| 信号机制（Signal） | Q8 | ●●● |
| 外核（Exokernel） | Q9 | ●● |

---

## 五、408 真题关联

| 真题年份/来源 | 相关 Q 号 | 涉及考点 |
| :--- | :--- | :--- |
| 2012 年 系统调用发生位置 | Q2 | 系统调用"可在用户态发生"的官方定性 |
| 进程状态转换题型 | Q13 | Running→Blocked 的触发事件判断 |
| I/O 软件层次题型 | Q4 | 四层结构与各层职责 |
| 用户态/核心态指令题型 | Q3 | 特权指令判断与常考干扰项 |
| 调度算法计算题型 | Q16 | 到达时间、周转时间、等待时间、响应时间的准确定义 |
| I/O 与 CPU 并行工作机制 | Q17 | DMA 搬运数据期间 CPU 继续计算，中断处理仅微秒级暂停 |
| 进程同步硬件实现 | Q18 | TestAndSet 原子指令、lock 变量的共享存储属性与独立管辖范围 |
| 同步机制全景对比 | Q19 | 事件驱动（锁/信号量/管程）vs 时间线型（屏障/锁步）的分类框架与本质区别 |
| PV 操作命名与同名陷阱 | Q20 | P=wait=down, V=signal=up。信号量 wait/signal ≠ 管程条件变量 wait/signal（同名异义） |

---

*汇总完成于 2026-06-06*
