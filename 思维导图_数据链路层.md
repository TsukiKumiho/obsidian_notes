# 数据链路层 虚拟思维导图

> 三层递进。第一二层写到 A4 上（约 15 个词），第三层留在书上做题时查。

---

## 第一层：第三章 8 个小节概览

- 数据链路层
    - 3.1 功能 — 封装成帧、差错控制、流量控制
    - 3.2 组帧 — 四种方法（字符计数/字符填充/零比特填充/违规编码）
    - 3.3 差错控制 — 检错（奇偶/CRC）→ 纠错（海明码）
    - 3.4 流量控制与可靠传输 — 停等→GBN→SR，滑动窗口核心
    - 3.5 介质访问控制 — 谁发？怎么不发冲突？（CSMA/CD / CSMA/CA / 令牌）
    - 3.6 局域网 — 以太网为主，VLAN、生成树
    - 3.7 广域网 — PPP、HDLC
    - 3.8 数据链路层设备 — 交换机、网桥、冲突域/广播域隔离

> 这一层的目标：合上书，看见这 8 个标题能说出每节大概讲什么。

---

## 第二层：每节的骨架（只记核心关系和要解决什么问题）

### 3.1 数据链路层的功能

- 3.1.1 所处的地位 — 网络层和物理层之间的桥梁
- 3.1.2 链路管理 — 建立/维持/释放数据链路
- 3.1.3 封装成帧与透明传输 — 关键：如何区分"帧边界"
- 3.1.4 流量控制 — 别让发送方淹没接收方
- 3.1.5 差错检测与可靠传输 — 错了要能发现/纠正

> 本节偏概念，做选择题时注意区分"链路层"和"物理层/网络层"各自的职责边界。

### 3.2 组帧

- 四种组帧方法（解决"怎么知道一帧的开始和结束"）
    - 3.2.1 字符计数法 — 首部标明长度，一个错全乱 → 基本不用
    - 3.2.2 字节填充法 — 用转义字符 DLE STX/ETX 区分数据和帧边界
    - 3.2.3 **零比特填充法** — 连续 5 个 1 后插一个 0（HDLC 用，**必考**）
    - 3.2.4 违规编码法 — 用物理层不用的编码组合作帧边界（如曼彻斯特编码中的无效电平跳变）

### 3.3 差错控制

- 3.3.1 检错编码 — 只检测出错，不纠正
    - 奇偶校验 — 奇校验/偶校验，最简单
    - **CRC（循环冗余校验）** — 只检错不纠错，生成多项式计算
- 3.3.2 纠错编码 — 能检测并纠正错误
    - **海明码** — 能检 2 位错，纠 1 位错
    - 海明距离 $d$ → 检 $d-1$ 位，纠 $(d-1)/2$ 位

### 3.4 流量控制与可靠传输 ★★★（本章核心）

- 3.4.1 流量控制与滑动窗口机制
    - **停等协议** — 发一帧等一帧。窗口=1，信道利用率 = 发送时间/(发送+传播)
    - **GBN（后退 N 帧）**
        - 累积确认，出错后从第一个未确认帧开始全部重发
        - 发窗口 $>1$，收窗口 $=1$
    - **SR（选择重传）**
        - 只重传出错的那一帧。需要逐个确认，收方需要缓存
        - 发窗口 = 收窗口，最大值 $\leq 2^{n-1}$
- 3.4.2 可靠传输机制
    - 确认帧（ACK/NAK）、超时重传、序号机制

> 核心表（必背）
> | 协议 | 发送窗口 | 接收窗口 | 确认方式 | 出错处理 |
> |:---|:---:|:---:|:---|:---|
> | 停等 | 1 | 1 | 逐帧确认 | 超时重传 |
> | GBN | $>1$ | 1 | **累积确认** | 从第一个未确认帧起全部重发 |
> | SR | $>1$ | $>1$ | **逐个确认** | 只重传出错帧 |

### 3.5 介质访问控制

- 3.5.1 信道划分（静态/无冲突）
    - 频分/时分/波分/码分 — 谁用什么预先分好，不冲突
    - CDMA：不同站用不同码片序列，正交分离
- 3.5.2 随机访问（动态/可能冲突）
    - **CSMA/CD**（有线以太网）
        - 先听后发 → 边发边听 → 冲突停发 → 随机退避
        - 争用期 $=2\tau$；最小帧长 $=2\tau \times$ 数据率
        - 截断二进制指数退避：$k=\min(重传次数,10)$，退避时间 $=2^k-1$ 个争用期
    - **CSMA/CA**（无线 802.11） — 详见 `思维导图示例_CSMA_CA.md`
        - 为什么不能 CD？→ 无线无法同时收发，隐藏站/暴露站问题
        - 核心机制：NAV + RTS/CTS + DIFS/SIFS + 随机退避
- 3.5.3 轮询访问（无冲突）
    - 令牌环 — 谁拿到令牌谁发

### 3.6 局域网

- 3.6.1 局域网基本概念和体系结构 — IEEE 802 系列
- 3.6.2 **以太网与 IEEE 802.3** ★★★
    - MAC 帧格式：目的MAC(6B) + 源MAC(6B) + 类型/长度(2B) + 数据(46-1500B) + FCS(4B)
    - 最小帧长 **64B**，最大帧长 **1518B**（含 FCS）
    - **无效 MAC 帧**：不是整数字节 / FCS 校验错 / 长度不在 64-1518 范围内
    - 10BASE-T / 100BASE-T / 吉比特以太网：全双工模式下不需要 CSMA/CD
- 3.6.3 **IEEE 802.11 无线局域网** ★★
    - CSMA/CA（见 3.5.2 及 `思维导图示例_CSMA_CA.md`）
    - 802.11 帧格式：**四个地址字段**（vs 以太网只有两个）
    - AP（接入点）、BSS、ESS 概念
- 3.6.4 **VLAN（802.1Q）**
    - 一个物理交换机拆成多个逻辑广播域
    - 帧中插入 4 字节 VLAN 标签（Tag Protocol ID + Priority + CFI + VID）
    - 交换机端口：Access 口（去掉 VLAN 标签）/ Trunk 口（保留标签）

### 3.7 广域网

- 3.7.1 广域网基本概念
- 3.7.2 **PPP（点对点协议）** — 目前最广泛使用的广域网数据链路层协议
    - 面向字节，全双工，不需要 CSMA/CD
    - LCP（链路控制协议）+ NCP（网络控制协议）两阶段建立链路
    - 帧首尾标志 F = 7E

### 3.8 数据链路层设备

- 3.8.1 网桥 — 连接两个同类型局域网，隔离冲突域
- 3.8.2 以太网交换机 — 多端口网桥
    - **自学习**：收到帧 → 记"源MAC+端口" → 查转发表
    - 目的 MAC **未知**或**广播** → **泛洪**（向除接收端口外的所有端口转发）
    - **直通交换**：只读目的 MAC（6 字节）即转发，延迟低但可能有错帧
    - **存储转发**：收完整帧，CRC 校验后再转发，延迟高但过滤错帧
- 3.8.3 共享式以太网 — 集线器时代，所有端口在同一个冲突域

- 冲突域 vs 广播域（必考）
    | 设备 | 隔离冲突域 | 隔离广播域 |
    |:---|:---:|:---:|
    | 集线器（物理层）| ❌ | ❌ |
    | 交换机/网桥（链路层）| **✅** | ❌ |
    | 路由器（网络层）| ✅ | **✅** |

---

## 第三层：做题时查，不用背

- CRC 生成多项式 → 计算时会给，原理要懂
- IEEE 802.3 MAC 帧的精确格式 → 做题翻书
- CDMA 码片序列计算 → 会算就行，不背码
- 截断指数退避的具体参数（$k=\min(重传次数,10)$ 等）
- DIFS/SIFS 的具体微秒数
- 生成树协议的三步过程（选根桥→选根端口→选指定端口）
- PPP 的 LCP 协商哪些选项
- 802.1Q VLAN 标签的 4 字节内部结构
- 交换机转发表的 MAC-端口映射细节

---

## 用这张导图的方法

1. 合上书，看第一层 8 个词 → 说出每节讲什么
2. 看第二层 → 合上书把滑动窗口三种协议的**窗口大小和确认方式**讲出来；把 CSMA/CD 和 CSMA/CA 的**核心区别**讲出来；把交换机"隔离什么不隔离什么"讲出来
3. 讲不清楚的地方 → 翻书看一遍，合上书再讲一次
4. 第三层永远不背——做题时翻书，哪条翻的次数多了自然记住

> 你的薄弱区：局域网(73%)主要是 MAC 帧格式和 VLAN 细节，设备(74%)主要是交换机转发规则。这两块在第二层多停留——能不看书写出 MAC 帧的 5 个字段（顺序+字节数）和交换机收到未知 MAC 时的"泛洪"动作，这两章就通了。
